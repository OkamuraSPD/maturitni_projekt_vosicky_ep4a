CREATE TABLE IF NOT EXISTS homes (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, home_password_hash TEXT NOT NULL, created_by_user_id INTEGER NOT NULL, created_at TEXT DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY(created_by_user_id) REFERENCES users(id) ON DELETE CASCADE);
